<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core"
         xmlns:f="/lib/form">
    <style>
        .not-impl {
            font-style:italic;
        }
        .selectorHelper,
        .selectorHelper + table > tbody > tr.optional-block-start.row-group-start.row-set-start {
            display:none;
        }
    </style>
    <div class="selectorHelper"><!--please do not remove this empty div--></div>

    <f:entry title="${%PRQA·Framework Installation name}"
             field="qafName">
        <f:textbox/>
    </f:entry>
    <f:entry title="${%PRQA·Framework Installation path}"
             field="qafHome">
        <f:textbox/>
    </f:entry>
</j:jelly>